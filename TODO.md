# TODO.md — MEE Stock

Flat checklist. See `PLAN.md` for the roadmap this is derived from, `memory.md` for full context/decisions.

## Frontend (apps/web) — all planned pages built

- [x] Auth (BFF httpOnly-cookie pattern), protected layout, `/dashboard`, `/materials` (list/search/pagination/deactivate/create with category dropdown)
- [x] Material Issue workflow UI (`/material-issues` list+tabs, `/material-issues/new` create with barcode-scan-to-add, `/material-issues/[id]` detail with role+status-aware actions)
- [x] Purchase Order + Goods Receive UI (`/purchase-orders` list+tabs+create+detail with status actions, `/goods-receives` list+create with PO-referenced fixed-item-table mode vs. no-PO free-picker mode, +detail)
- [x] Reports UI (`/reports` index, `/reports/stock-value`, `/reports/issue-history` — pure Server Components, GET filters only)
- [x] Print support — `PrintButton` + `@media print` CSS on the 3 document detail pages (material issue, PO, goods receive)
- [x] Access Control admin UI (`/users` list+search, `/users/new` create, `/users/[id]` update + site-access assign/revoke, self-lockout guard mirrored in the UI)

## Done this session (for reference, see memory.md for full detail)

- [x] Cost-visibility gap closed (STAFF no longer sees `unitCost`/`standardCost` on stock-ledger/material-issue endpoints)
- [x] `/categories` endpoint added (list + create), wired into `/materials/new` dropdown
- [x] `/categories` update/delete (edit gated `WAREHOUSE`, delete blocked while in-use, dedicated `/categories` management page) — see `PLAN.md`
- [x] `packages/shared-types` created and wired into `apps/web`
- [x] Fixed a self-inflicted test-doubling bug (`apps/api/dist/` polluting Vitest's file discovery) and hardened `vitest.config.ts` against recurrence
- [x] `GET /warehouses`, `GET /materials/by-code/:code`, `GET /suppliers` endpoints added
- [x] **Security fix**: `passwordHash` was leaking via 4 endpoints through un-`select`-ed Employee relations — fixed with shared `employeeRefSelect`
- [x] Material Issue workflow UI, full stack (backend endpoints + frontend pages), designed collaboratively with the user (auto-derived project, one-click approve, barcode-scan-ready item picker) and verified end-to-end
- [x] Purchase Order + Goods Receive UI, full stack, verified end-to-end including partial-receive → PO auto-status-derivation and the fixed-vs-free item-picker mode split
- [x] Reports UI (`/reports/stock-value`, `/reports/issue-history`), fully curl-verified since these are pure Server Components with no Server Actions involved
- [x] Access Control admin UI, full stack, verified end-to-end including self-lockout guard rendering and a refinement of the bound-action curl-verification rule (see `PLAN.md`)
- [x] **Role decision resolved**: added `PURCHASING` role (schema migration + seed account + backend re-gating of PO endpoints + frontend role checks/dropdown), strictly separate from `WAREHOUSE` (which keeps Goods Receive but lost PO access) — verified end-to-end via curl and the actual UI
- [x] **Demo/sample data**: `apps/api/prisma/seedDemo.ts` (run via `pnpm run seed:demo`) — 3 new sites, 9 material issues (3/site, covering all 5 possible statuses incl. a real shortfall case), 5 purchase orders (covering all 5 lifecycle stages). Driven through the real service layer, not raw inserts, so it's fully rule-compliant demo data
- [x] **SLA decision resolved**: badge-only "เกินกำหนด" overdue flag, env-configurable (`MATERIAL_ISSUE_APPROVAL_SLA_HOURS`/`MATERIAL_ISSUE_FULFILLMENT_SLA_DAYS`, defaults 24h/3d), computed at read time from existing timestamps — no schema migration needed. Verified by backdating a real record and confirming the flag flips. `next build` re-check (deferred earlier since the dev server was busy) now done — clean build, `/material-issues` and `/material-issues/[id]` included
- [x] **Barcode decision resolved**: user confirmed real scanning will be manufacturer EAN/UPC barcodes (not self-printed SKU labels), so added a dedicated nullable+`@unique` `Material.barcode` field (migration `add_material_barcode`) rather than continuing to overload `code`. `GET /materials/by-code/:code` now checks `barcode` first, falls back to `code` — no frontend changes needed to `ItemsField`/`CostedItemsField`, they already call this endpoint. Added barcode input to `/materials/new`, shown as a hint line under the code in the `/materials` list. Independent duplicate-`CONFLICT` checks for both fields. Verified live via curl: create-with-barcode, scan-by-barcode, scan-by-code fallback, and duplicate-barcode 409 all confirmed against the running dev API; typecheck (api/web/shared-types) and full Vitest suite (20/20) pass
- [x] **Real-browser verification (Playwright)** — installed Playwright + Chromium as a devDependency in `apps/web` (kept for future reuse) and scripted all 8 previously-unverified items: the 4 unbound create-forms (`createMaterialAction`, `createMaterialIssueAction`, `createPurchaseOrderAction`, `createUserAction`) plus print-preview CSS on the 3 document detail pages. All 8 pass. **Found and fixed a real bug along the way**: `generateDocNo()` in `materialIssueService.ts`/`purchaseOrderService.ts`/`goodsReceiveService.ts` used `COUNT(*)+1` instead of `MAX(suffix)+1` — once any gap existed in the day's sequence (a failed create, a race), the count permanently undercounted and every subsequent create collided on the same already-used docNo forever (not a one-off, reproduced live via the browser). Fixed by switching all 3 to `findLatestDocNoWithPrefix` + `extractDocNoSuffix` (self-healing regardless of gaps — new `lib/docNoSequence.ts`), plus added `lib/docNoRetry.ts#createWithDocNoRetry` around each create call to also absorb genuine concurrent-request races. Typecheck + Vitest (20/20) pass after the fix; re-ran the full Playwright suite to confirm all 8 checks green. (The goods-receive print-CSS check initially "failed" too, but that was a bad test assumption on my part — `GoodsReceive` has no lifecycle/status by design, so no badge to check; fixed the test, not the product.)
- [x] **pg deprecation warning silenced** — root cause is inside `@prisma/adapter-pg`'s own relation-batching internals (confirmed via `--trace-deprecation` stack trace), not fixable in our code, and we're already on the latest stable Prisma (7.8.0). Since there's no real fix available, added a targeted `process.on("warning")` filter in `src/test/setupEnv.ts` that suppresses only this one known-benign message (matched by exact text) — required `process.removeAllListeners("warning")` first since Node's default stderr-printing warning handler is just another listener, not something a second listener overrides on its own. All other deprecation warnings still print normally. Verified: Vitest (20/20) passes with the warning gone from output.
- [x] **Category CRUD completed**: `PATCH`/`DELETE /categories/:id` (delete blocked while in-use, edit gated `WAREHOUSE`) + dedicated `/categories` management page — full detail in `PLAN.md`
- [x] **Real Landmark data rechecked & adopted as source of truth**: `meestock_dev` holds the real 556-material Project Landmark import, not seed fixtures; cross-verified the zero-cost/zero-balance materials against the raw spreadsheet (genuine source gaps, not import bugs) — full detail in `memory.md`
- [x] **3-site test employees + full workflow simulation**: restored the 5 missing seed employees (wiped by an earlier `clearTestData` run), built 2 new test SITE warehouses alongside the existing one, and ran the real PO→receive→issue→approve→fulfill cycle end-to-end via the live API for all 3 (including a deliberate shortfall/`PARTIALLY_FULFILLED` demo on site 3) — full detail in `PLAN.md`/`memory.md`
- [x] **`/materials` category filter**: added a `categoryId` `<select>` next to the search box (plain GET form field, no JS) — backend already supported the query param, so this was frontend-only. Verified live: filtering narrows results correctly and the dropdown retains its selection
- [x] **Deployed to production**: Railway (`apps/api` + Postgres) at https://meestock-api-production.up.railway.app, Vercel (`apps/web`) at https://meestock.vercel.app. Found and fixed 3 real deploy-blocking bugs along the way — see `PLAN.md`/`memory.md`. Verified end-to-end: login + dashboard render correctly against the live production stack
- [x] **Production-readiness gap check**: found and fixed 2 real bugs plus closed the 4 security gaps flagged in the readiness review — see `PLAN.md`/`memory.md` for full detail. Test suite is 20/20 again (was silently 7/20 broken — untested migration drift), typecheck/build both clean on api+web
- [x] **Mobile/tablet support**: responsive layout (sidebar collapses to a horizontal icon bar under 900px, tables become horizontally scrollable, bento-grid/two-col reflow to 1-2 columns) + camera-based barcode scanning (`components/CameraScanButton.tsx`, `@zxing/browser`) as an added option alongside the existing external-HID-scanner input on `/material-issues/new` and the PO/goods-receive item pickers. Verified visually via Playwright at phone/tablet/desktop viewports — see `PLAN.md`/`memory.md`
